# Required
ANTHROPIC_API_KEY=your-api-key-here

# Optional: give the agent a specific task to start with.
# When unset, the agent starts with no task (true tabula rasa mode).
# INITIAL_TASK=write a haiku about recursion

# Optional: tune the loop behavior
# MAX_TURNS=200
# MAX_TOOL_CALLS_PER_TURN=20
# MODEL=claude-opus-4-6
